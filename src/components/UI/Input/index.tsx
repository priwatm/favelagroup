import * as React from "react";

export interface InputProps {
    placeholder: string;
    value: string;
    type: string;
}
export const Input: React.ComponentType<InputProps> = ({type, value, placeholder}) =>{
    return(
        <>
        </>
    )
}
